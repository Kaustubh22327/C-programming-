#include<stdio.h>
    int main(){
    int a;   
        printf("Enter the side :");
        scanf("%d", &a);
        int area= a * a;
        printf("The area of square is %d",area);
        return 0;
    }